#ifndef components_divisor_h
#define components_divisor_h

// This function divides two numbers and returns the result

double divisor(double numerator, double denominator)
{
    return numerator / denominator;
}

#endif